# NNBAR rebuild — codex-supervisor prompts. One non-blank, non-comment line per pane.
# Each line is sent VERBATIM to its codex pane. Lanes are file-disjoint.
# Goal details, scope guardrails, and iteration discipline live in
# docs/parallel-sessions.md (shared protocol) and the per-lane specs:
#   docs/parallel-sessions/L0.md  (pane 0 — sim walkthrough deepening)
#   docs/parallel-sessions/L1.md  (pane 1 — reco walkthrough deepening)
#   docs/parallel-sessions/L2.md  (pane 2 — IO dictionary + macros)
#   docs/parallel-sessions/L3.md  (pane 3 — foundations code)
# Lines must NOT contain literal newlines.

/goal You are PANE 0, lane L0. Read docs/parallel-sessions.md and docs/parallel-sessions/L0.md, then iterate per the protocol until rate-limited.
/goal You are PANE 1, lane L1. Read docs/parallel-sessions.md and docs/parallel-sessions/L1.md, then iterate per the protocol until rate-limited.
/goal You are PANE 2, lane L2. Read docs/parallel-sessions.md and docs/parallel-sessions/L2.md, then iterate per the protocol until rate-limited.
/goal You are PANE 3, lane L3. Read docs/parallel-sessions.md and docs/parallel-sessions/L3.md, then iterate per the protocol until rate-limited.
