# Lane L1 — reconstruction atomic walkthrough deepening

**Branch:** `lane/L1-reco-walkthrough`
**Worktree:** `/Volumes/MyDrive/nnbar/nnbar/simulation-L1`
**Repo:** orchestration repo (this one)
**Writable target:** `docs/rebuild_plans/08_reconstruction_atomic_walkthrough.md`
**Read-only:** everything else, especially `NNBAR_Detector/nnbar_reconstruction/`.

## Goal

Populate the stub sections of plan 08 with file-path + line-number
forensic detail for every public function in
`NNBAR_Detector/nnbar_reconstruction/*.py`.

Sections targeted:

- **§3.3 vertex** — vertex-reconstruction functions inside
  `reconstruction.py`.
- **§3.4 charged objects** — `reconstruct_charged_objects` and helpers.
- **§3.5 photon / π⁰** — clustering, charged/neutral, photon objects,
  π⁰ pairing, kinematic-fit-style corrections inside
  `reconstruction.py`.
- **§6 validation** — `validation.py` public surface
  (`evaluate_reconstruction_truth`, `aggregate_reconstruction_truth`,
  `assess_validation_readiness`).
- **§7 π⁰ study** — `pi0_study.py` (1974 lines; expect to need
  multiple iterations).
- **§8 charged study** — `charged_study.py` (2241 lines; multiple
  iterations).
- **§9 π⁰ fake study** — `pi0_fake_study.py`.

For each public function: input columns (cite plan 09 Class A/B/C),
output columns, decision rule with hardcoded constants and line
numbers, truth-column reads (if any) flagged by §3.7 of plan 08.

## Working rhythm (per iteration)

1. Re-read `docs/parallel-sessions.md` and this file.
2. Pick the next module section that lacks detail.
3. Read its module file end-to-end. For 1500+ line modules, focus on
   one logical group of public functions per iteration.
4. Write the §-entries in `08_reconstruction_atomic_walkthrough.md`
   with line-number citations.
5. Verify file size ≤ 500 lines. If approaching, split per "splitting
   plan 08" below.
6. `git add docs/rebuild_plans/08_reconstruction_atomic_walkthrough.md`
7. `git commit -m "docs(plan-08): document <module-or-section>" -m "Plan: 08" -m "Lane: L1"`
8. `bash /Volumes/MyDrive/nnbar/nnbar/simulation/scripts/codex-supervisor/merge.sh lane/L1-reco-walkthrough`
9. Continue.

## Splitting plan 08

Plan 08 will exceed 500 lines after the §7/§8 deep dives.
When approaching:

- Create `docs/rebuild_plans/08_reconstruction_atomic_walkthrough/`.
- Move §7 → `08_7_pi0_study.md`, §8 → `08_8_charged_study.md`,
  §9 → `08_9_pi0_fake_study.md`.
- Optionally split §3.3, §3.4, §3.5 if the main file still exceeds
  500 lines.
- The main file keeps its YAML header, §1–§2, §3.1–§3.2, the §3.3
  stub linking to splits, and §10–§15.
- Each split file carries its own YAML header with `id` like `08_7`
  and `depends_on: [08_reconstruction_atomic_walkthrough]`.

Commit the split as `refactor(plan-08): split for 500-line cap`.

## Required reading (first iteration only)

- `CODING_STANDARDS.md` (especially §1 the 500-line rule)
- `docs/rebuild_plans/00_README.md`
- `docs/rebuild_plans/01_realism_contract.md` (the Class A/B/C scheme
  drives §3.7 truth-leakage flagging)
- `docs/rebuild_plans/08_reconstruction_atomic_walkthrough.md` (your
  starting point)
- `NNBAR_Detector/docs/reconstruction.md` (companion technical
  reference; complements but does not replace plan 08)

## Stop condition

Every public function in `NNBAR_Detector/nnbar_reconstruction/*.py`
has a § entry. After that, stop and surface a status note in your
final commit message.

## Out of scope

- Editing `NNBAR_Detector/nnbar_reconstruction/*.py` (read-only).
- Editing other plans (L0/L2/L3 own those).
- Implementing audit/registry/statistics code (L3 owns those).
