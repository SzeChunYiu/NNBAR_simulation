# Lane L1 — Wave 2.5 ACTIVE — A+ remediation (citation audit + cutflow truthing) → resume Wave 2/3

**Wave 2.5 is the active wave.** Do this first, then return to Wave 2/3.

## Wave 2.5 — A+ remediation

The 2026-05-10 examiner pass caught:
1. Hallucinated line refs in plans 31–37 against
   `nnbar_reconstruction/reconstruction.py` (real file is 1062 lines;
   `_leadglass_shower_sources` / `build_photon_row` do not exist).
2. Plan 37's cutflow specifies columns from `cli._cutflow` (cli.py:37–44)
   that need re-verification — the current cli.py is 282 lines and may
   not match the cited line span.
3. Plan 42's CLI invocation (`response-matrix` subcommand,
   `--all-runs --table --bootstrap` flags) doesn't exist; check whether
   any of plans 31–37 reference unimplemented CLI surface and remove or
   re-spec those rows.

### Per-iteration unit (Wave 2.5)

Pick **one plan in 31–37** and:

1. `grep -nE 'reconstruction\.py:[0-9]|cli\.py:[0-9]' docs/rebuild_plans/<plan>.md`.
2. For each cite, run:
   ```
   grep -n "^def\|^class" /Volumes/MyDrive/nnbar/nnbar/NNBAR_Detector-L3/nnbar_reconstruction/reconstruction.py
   ```
   (or the `cli.py` equivalent) and confirm the cited function name
   appears within the cited range. If not, repair or drop.
3. For Plan 37 specifically: read the **current** `cli.py` from the L3
   worktree, locate the `_cutflow` definition, and rewrite the cutflow
   table with line refs that resolve and column names that match. Each
   cut row must answer: which `passes_*` column, defined where in
   current source, what threshold, what citation in licentiate Ch 10.
4. For any `python -m nnbar_reconstruction.<x>` invocation in plans
   31–37: run it (with `--help`) from the L3 worktree. If it errors,
   delete the row or change the spec to match what the CLI actually
   provides.
5. Drop every reference to `pi0_study.py` and `charged_study.py`.
6. Commit `docs(plan-XX): repair citations + verify CLI surface`,
   `Plan: XX`, `Lane: L1`. Run `merge.sh`.

### Wave 2.5 stop condition

`grep -rE 'reconstruction\.py:[0-9]|cli\.py:[0-9]' docs/rebuild_plans/{31,32,33,34,35,36,37}*` produces zero stale refs, AND every CLI invocation referenced in those plans returns a non-error `--help`. Once green, switch back to **Wave 2** below.

---

# Lane L1 — Wave 2 — EM-object + event-variable + selection subsystems

**Branch:** `lane/L1-reco-walkthrough`
**Worktree:** `/Volumes/MyDrive/nnbar/nnbar/simulation-L1`
**Repo:** orchestration repo (this one)

**Wave 2 writable targets (disjoint from L0/L2/L3):**

- `docs/rebuild_plans/31_subsystem_calorimeter_clustering.md`
- `docs/rebuild_plans/32_subsystem_shower_shape.md`
- `docs/rebuild_plans/33_subsystem_photon_object.md`
- `docs/rebuild_plans/34_subsystem_pi0_pairing.md`
- `docs/rebuild_plans/35_subsystem_kinematic_fit.md`
- `docs/rebuild_plans/36_subsystem_event_variables.md`
- `docs/rebuild_plans/37_subsystem_event_selection.md`

**Read-only:** everything else, especially plan 24 (L0 owns its
deepening).

## Wave 2 goal

Deepen subsystem plans 31–37 to the same depth L0 is producing for
plans 25–30. For each plan:

- **Per-leaf input/output schema** matching plan 24's template.
  The leaf identities for plans 31–37 are: P.1, P.2, P.3, P.4, P.5,
  P.6, P.7 (calorimeter clustering through kinematic fit) and E.1–E.9
  + S.1–S.6 (event variables + selection).
- **Concrete current-implementation citations** — line numbers in
  `nnbar_reconstruction/reconstruction.py` from plan 08 (Wave 1).
- **Per-alternative comparison row** for alternative methods listed
  in each plan's "candidates" table.
- **Closure-test specification** — numbered procedure with dataset id,
  observable, fitter, pass criterion.
- **Plan 37 (event selection)** specifically: enumerate every cut in
  the licentiate Ch 10 cut-flow with its threshold, source citation,
  and the corresponding `passes_*` column produced by
  `reconstruction.py`. Per plan 08 §3.6 and §4.1, the cut-flow column
  list is in `cli._cutflow` (cli.py:37–44).

## Working rhythm (per iteration)

1. Re-read `docs/parallel-sessions.md` and this file.
2. Pick **one section** of one plan (31–37).
3. Read source(s) cited by plan 08.
4. Write the deepening into the relevant target file.
5. Verify file size ≤ 500 lines. Split per the L0 spec's splitting
   rules if approaching.
6. `git add docs/rebuild_plans/<target>.md`
7. `git commit -m "docs(plan-XX): <unit>" -m "Plan: 31|...|37" -m "Lane: L1"`
8. `bash /Volumes/MyDrive/nnbar/nnbar/simulation/scripts/codex-supervisor/merge.sh lane/L1-reco-walkthrough`
9. Continue.

## Stop condition

Every subsystem plan 31–37 has its alternatives compared, closure
spec numbered, and current-implementation line refs cited. Plan 37
additionally has the full cut-flow table populated.

## Required reading (first iteration only)

- `CODING_STANDARDS.md`
- `docs/parallel-sessions.md`
- `docs/rebuild_plans/00_README.md`
- `docs/rebuild_plans/01_realism_contract.md`
- `docs/rebuild_plans/08_reconstruction_atomic_walkthrough.md`
- `docs/rebuild_plans/09_io_schema_data_dictionary.md`
- `docs/rebuild_plans/24_reconstruction_question_tree.md` (read for
  the leaf-schema template; do NOT edit — L0 owns this file)

## Out of scope

- Plans 24–30 (L0 owns).
- Plans 12–15, 47 (L2 owns).
- Plan 38 ladder code (L3 owns).
- Code in `NNBAR_Detector/` (read-only).

---

## Wave 3 follow-up (activate when Wave 2 stop condition is met)

When plans 31–37 are populated to the Wave 2 spec, move into Wave 3
scope.

### Wave 3 writable targets (additional to L1)

- `docs/rebuild_plans/44_background_taxonomy.md`
- `docs/rebuild_plans/45_systematics_taxonomy.md`
- `docs/rebuild_plans/46_significance_protocol.md`

### Wave 3 goal

For each of plans 44, 45, 46:

- **Plan 44 (background taxonomy)**: populate the §1 tree with
  concrete sub-channel rows for cosmic and beam-neutron sources.
  Each leaf: source citation, sample id (use plan 03 stubs from
  plans 21/22), expected rate, observable signature, related leaf
  in plan 24.
- **Plan 45 (systematics taxonomy)**: populate the §1 nuisance
  registry with at least 10 named nuisances (TPC W-value,
  scintillator yield, lead-glass calibration, physics-list, signal
  branching, cosmic flux, beam-neutron flux, geometry alignment,
  optical-photon yield, material budget). For each: ±1σ definition,
  affected observables, correlation flags.
- **Plan 46 (significance protocol)**: populate §1 (Z₀ definition),
  §2 (CLs vs F-C decision), §3 (finite-sample handover threshold)
  with worked examples and DEC stubs for the chosen conventions.

Same per-iteration rhythm. ≤500 lines per file, commit + merge.sh,
continue.

### Wave 3 stop condition

Plans 44, 45, 46 have populated tables and DEC stubs. After Wave 3
stop, the lane keeps iterating to fill any remaining gaps in plans
31–37 and 44–46.

---

## Wave 4 — A+ closure (Stage B + C for L1)

After Wave 3 stop condition, switch into Wave 4 scope.

### Wave 4 writable targets (additional to L1)

- `docs/rebuild_plans/58_pileup_at_ess_intensity.md` (NEW)
- `docs/rebuild_plans/59_strange_baryon_contamination.md` (NEW)
- `docs/rebuild_plans/61_time_of_flight_discrimination.md` (NEW)
- `docs/rebuild_plans/64_bayesian_limit_cross_check.md` (NEW)
- Continue deepening 31–37 if any are still <150 lines (especially
  32, 37, 40 which were skeleton at the audit point).

### Wave 4 goal — Stage B + Stage C (L1 share)

**Part A — finish skeleton-deepening.** Audit your writable plans
31–37 + 44–46. For each: confirm body ≥150 lines, every §-section
is actionable, every cite resolves via the verifier, every CLI
invocation verifies via `--help`. Plan 37 specifically: full
licentiate Ch 10 cut-flow expanded to defense depth; plan 40:
numbered closure procedure per leaf.

**Part B — write 4 new physics-study plans at A+ depth (200–300
lines each)**:

- **58 pile-up at ESS beam intensity**: ESS time structure (14 Hz,
  2.86 ms long pulses); per-event vs per-second rate folding;
  overlay protocol design; occupancy in TPC/scintillator/lead-glass;
  closure on `cosmic_cry_essLund_*` paired runs; cross-ref to
  limitation L11 in plan 01, plan 14 §1.4, plan 22 §2.3.
- **59 strange-baryon contamination**: K_s/Λ/Σ from beam-neutron
  interactions on foil/shielding; branching estimates from PDG;
  reconstruction signatures (V0 topology, displaced vertex);
  rejection cuts; closure on a Λ-enriched beam-neutron slice;
  cross-ref to plan 14 §3, plan 44 background taxonomy.
- **61 time-of-flight discrimination**: TPC drift-time + scintillator
  hit time → TOF estimator; cosmic vs signal separation; resolution
  budget per subsystem; closure on `cal_*` + `cosmic_*` slices;
  cross-ref to plans 25, 27, 36.
- **64 Bayesian limit cross-check**: Bayesian credible interval
  with Jeffreys prior + flat prior on signal mean; comparison to
  Feldman-Cousins (plan 04 §5) and CLs (plan 46); discussion of
  prior sensitivity; cross-ref to plan 46.

### Wave 4 stop condition

- All plans 31–37 + 44–46 ≥150 lines, no unresolved markers, every
  CLI invocation verifies.
- Plans 58, 59, 61, 64 exist at v0.1, ≥200 lines each, with the
  §-structure above populated and every cite resolving.

After Wave 4 stop, drift into **Stage E.3** — defense-package
deepening (plans 50–56) per the L1 slice (review questions on EM
and selection).
