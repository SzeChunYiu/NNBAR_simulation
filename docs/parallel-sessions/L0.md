# Lane L0 — Wave 2.5 ACTIVE — A+ remediation (citation audit) → resume Wave 2/3

**Wave 2.5 is the active wave.** Do this first, then return to Wave 2/3.

## Wave 2.5 — A+ remediation (citation audit)

The 2026-05-10 examiner pass caught hallucinated line refs in plans
**24–30**. Every cite of the form `reconstruction.py:N-M` or a function
name like `_leadglass_shower_sources` / `build_photon_row` /
`reconstruct_charged_objects` must be verified against the live source
in `/Volumes/MyDrive/nnbar/nnbar/NNBAR_Detector-L3/nnbar_reconstruction/reconstruction.py`
(file is **1062 lines**, not 1764).

### Per-iteration unit (Wave 2.5)

Pick **one plan** in 24–30 and:

1. `grep -nE 'reconstruction\.py:[0-9]' docs/rebuild_plans/<plan>.md`
   to list every numeric cite.
2. For each cite `<func>` at `:N` or `:N-M`:
   - `grep -n "^def <func>\|^    def <func>" /Volumes/MyDrive/nnbar/nnbar/NNBAR_Detector-L3/nnbar_reconstruction/reconstruction.py`
   - If the grep returns no match, **the function does not exist**:
     remove the cite and replace with a TODO naming the actual current
     function that owns this responsibility (use plan 08's deepened
     walkthrough to find the real owner).
   - If the grep returns a line number, replace the cite with the
     real `:N` (or `:N-M` covering the function body — bound the upper
     end at the next `^def` line).
3. For each cite to a function name `_leadglass_shower_sources`,
   `build_photon_row`, etc. — confirm via grep whether it exists. If
   not, drop the reference entirely.
4. Drop every reference to `pi0_study.py` and `charged_study.py` from
   plan bodies — those files do not exist.
5. Update `last_updated:` in the plan frontmatter to today.
6. Commit with message: `docs(plan-XX): repair line-ref citations`,
   trailers `Plan: XX`, `Lane: L0`, plus a body line listing how many
   cites were fixed and how many were dropped.
7. Run `merge.sh lane/L0-sim-walkthrough`.

### Wave 2.5 stop condition

`grep -rE 'reconstruction\.py:[0-9]' docs/rebuild_plans/{24,25,26,27,28,29,30}*` produces zero stale refs (every cite resolves to a real function within range). Once green, switch back to **Wave 2** below.

---

# Lane L0 — Wave 2 — reconstruction decomposition + tracking subsystems

**Branch:** `lane/L0-sim-walkthrough`
**Worktree:** `/Volumes/MyDrive/nnbar/nnbar/simulation-L0`
**Repo:** orchestration repo (this one)

**Wave 2 writable targets (disjoint from L1/L2/L3):**

- `docs/rebuild_plans/24_reconstruction_question_tree.md`
  (and any new files under `docs/rebuild_plans/24_reconstruction_question_tree/`
  if the 500-line cap forces a split)
- `docs/rebuild_plans/25_subsystem_tpc_hits_to_tracks.md`
- `docs/rebuild_plans/26_subsystem_track_fit_and_pulls.md`
- `docs/rebuild_plans/27_subsystem_dedx.md`
- `docs/rebuild_plans/28_subsystem_range_and_stopping.md`
- `docs/rebuild_plans/29_subsystem_charged_pid.md`
- `docs/rebuild_plans/30_subsystem_vertex.md`

**Read-only:** plan 07/08/09/10 (Wave 1 already deepened them) and
all other plans, code, and per-lane specs.

## Wave 2 goal

Two-part pass:

### Part A — deepen plan 24 (reconstruction question tree)

For each leaf in §2 (Vertex), §3 (Charged), §4 (Photon/π⁰), §5 (Event
variables), §6 (Selection), populate the per-leaf input/output schema
template that lives in §2.1 currently as a stub. Each leaf needs:

```
Leaf <ID> (e.g. V.4 vertex aggregation)
  inputs (Class A): list of parquet columns from plan 09
  forbidden (Class B): list of truth columns
  decision rule: one paragraph
  output schema: {field: dtype, ...}
  allowed truth use: validation_only / diagnostic_only / labeling
  downstream consumers: subsystem plan IDs
```

The full leaf list is already in plan 24 §2–§6. Plan 09 (deepened in
Wave 1) supplies the column classifications.

### Part B — deepen subsystem plans 25–30 (tracking + charged-PID + vertex)

Each plan currently has the v0.1 skeleton (charter, current
implementation, alternatives, closure, acceptance). Wave 2 adds:

- **Per-leaf input/output schema** matching plan 24's template.
- **Concrete current-implementation citations** — line numbers in
  `nnbar_reconstruction/reconstruction.py` (use plan 08, deepened in
  Wave 1, as the source of truth for line refs).
- **Per-alternative comparison row** — when the plan lists 2+
  alternatives in its "candidates" table, add one row per alternative
  with: source paper / codebase, NNBAR-specific adaptation note,
  expected ladder leaf delta (qualitative until plan 38 lands).
- **Closure-test specification** — turn the §closure paragraph into
  a numbered procedure: dataset id from plan 03, observable, fitter,
  pass criterion.

## Working rhythm (per iteration)

1. Re-read `docs/parallel-sessions.md` and this file.
2. Pick **one leaf** in plan 24 OR **one section** of plans 25–30.
3. Read source(s) cited by plan 08 (reconstruction walkthrough).
4. Write the deepening into the relevant target file.
5. Verify file size ≤ 500 lines. Split per "splitting" below.
6. `git add docs/rebuild_plans/<target>.md`
7. `git commit -m "docs(plan-XX): <unit>" -m "Plan: 24|25|...|30" -m "Lane: L0"`
8. `bash /Volumes/MyDrive/nnbar/nnbar/simulation/scripts/codex-supervisor/merge.sh lane/L0-sim-walkthrough`
9. Continue.

## Splitting

Plan 24 may exceed 500 lines after every leaf gets a schema.
When approaching:

- Create `docs/rebuild_plans/24_reconstruction_question_tree/` directory.
- Move per-branch leaves into per-branch files:
  `24_2_vertex.md`, `24_3_charged.md`, `24_4_photon_pi0.md`,
  `24_5_event_variables.md`, `24_6_selection.md`.
- Main file keeps §1, §7 (truth-leakage gate), §8 (visualisation).
- Each split file carries its own YAML header.

Subsystem plans 25–30 follow the same pattern if they exceed.

## Stop condition

- Every leaf in plan 24 §2–§6 has its input/output/decision-rule
  schema populated.
- Every subsystem plan 25–30 has its alternatives compared, closure
  spec numbered, current-implementation line refs cited.

## Required reading (first iteration only)

- `CODING_STANDARDS.md`
- `docs/parallel-sessions.md`
- `docs/rebuild_plans/00_README.md`
- `docs/rebuild_plans/01_realism_contract.md`
- `docs/rebuild_plans/08_reconstruction_atomic_walkthrough.md` (Wave 1
  output — line-ref source)
- `docs/rebuild_plans/09_io_schema_data_dictionary.md` (column-class
  source)
- `docs/rebuild_plans/24_reconstruction_question_tree.md` (current state)

## Out of scope

- Plans 31–37 (L1 owns those).
- Plans 12–15, 47 (L2 owns).
- Code in `NNBAR_Detector/` (read-only).
- Implementing the ladder (L3 owns plan 38 code).

---

## Wave 3 follow-up (activate when Wave 2 stop condition is met)

When all of plan 24's leaves and plans 25–30 are populated to the
Wave 2 spec, **do not stop**. Move into Wave 3 scope, additive to
your writable targets:

### Wave 3 writable targets (additional to L0)

- `docs/rebuild_plans/41_n_minus_1_and_roc_studies.md`
- `docs/rebuild_plans/42_unfolding_protocol.md`
- `docs/rebuild_plans/43_signal_efficiency.md`

### Wave 3 goal

For each of plans 41, 42, 43:

- **Concrete procedure**. Replace each numbered step in the plan's
  closure / protocol section with a precise, runnable procedure: what
  CLI to invoke, what parquet columns to read, what intermediate
  artifact to save, what assertion to make. Use plan 09 (data
  dictionary) and plan 38 (ladder leaves) as references.
- **Specific tolerances**. Plans 41/42/43 currently quote tolerances
  in vague form (e.g. "K-S p > 0.01"). Wave 3 turns these into
  per-observable rows: observable name, tolerance value, rationale
  citation.
- **Cross-references**. Wire each plan to the relevant ladder leaf
  (plan 38), the relevant subsystem (plans 25–37 deepened in Wave
  2), and the ledger row template (plan 47 §1).

Same per-iteration rhythm as Wave 2: one section per iteration, ≤500
lines per file, commit + merge.sh, continue.

### Wave 3 stop condition

Plans 41, 42, 43 have concrete procedures and tolerance tables in
every closure / protocol section. After Wave 3 stop, queue any
remaining gaps in the L0 stop conditions; the lane keeps iterating
until the plan-set has zero open §-stubs in its writable targets.
