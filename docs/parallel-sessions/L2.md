# Lane L2 — Wave 2.5 ACTIVE — A+ remediation (bib audit + ledger truthing) → resume Wave 2/3

**Wave 2.5 is the active wave.** Do this first, then return to Wave 2/3.

## Wave 2.5 — A+ remediation

The 2026-05-10 examiner pass caught two failure classes for L2:

1. **Bib citations may be hallucinated.** Plans 12–15 cite papers
   (Amsler & Myhrer 1991, Friedman & Gal 2007, etc.) without
   confirming the cite key resolves in
   `/Users/billy/Desktop/projects/overleaf-hibeam-thesis/ref.bib`.
2. **Reproduction ledger is seeded but unrun.** Every row in
   `data/ledger/rows.yml` has `status: not-attempted` — zero
   thesis-figure values have been re-derived. An A+ ledger has at
   least a handful of `status: reproduced` rows with a delta column
   showing rebuild-vs-thesis agreement (or honest `mismatch` with the
   delta and a hypothesised cause).

### Per-iteration unit (Wave 2.5) — two sub-tasks, alternate

**Sub-task A — bib audit** (one pass per plan in 12–15):

1. `grep -E '\\cite\{|\(([A-Z][a-z]+ [&] [A-Z][a-z]+|et al\.) [0-9]{4}\)' docs/rebuild_plans/<plan>.md`
   to list every citation form.
2. For each citation:
   - If `\cite{key}`: `grep "^@.*{key,"
     /Users/billy/Desktop/projects/overleaf-hibeam-thesis/ref.bib`.
     No match → replace with TODO and the missing-key name.
   - If author-year prose: convert to `\cite{key}` form using the bib
     file. If no key exists, mark TODO and stop citing the paper as
     fact.
3. Update plan frontmatter `last_updated:` and commit:
   `docs(plan-XX): verify bib citations against ref.bib`,
   `Plan: XX`, `Lane: L2`.

**Sub-task B — ledger truthing** (one ledger row per iteration):

1. Pick one row in `data/ledger/rows.yml` whose
   `reproducing.command` looks runnable today (i.e. references a CLI
   that actually exists per L1's CLI verifier — start with
   `summarize`).
2. Find the simulation-output directory the row points to. If it
   exists locally under `/Volumes/MyDrive/nnbar/nnbar/NNBAR_Detector*/output/`,
   run the command, record the output, and compare to
   `thesis_value`. Update the row:
   - `status: reproduced` if within stated tolerance (or if no
     tolerance, ±5%).
   - `status: mismatch` with `delta:` field if outside.
   - `reproduced_value:` always populated.
3. If no local sample exists for that row, mark the row
   `status: blocked-no-sample` and move on to a row that has data.
4. Commit `docs(ledger): reproduce <id>`, `Plan: 47`, `Lane: L2`.

### Wave 2.5 stop condition

- Zero unresolved `\cite{...}` keys across plans 12–15 (each one
  either resolves in `ref.bib` or has been replaced with a TODO).
- ≥5 ledger rows in `data/ledger/rows.yml` carry
  `status: reproduced` or `status: mismatch` with non-empty
  `reproduced_value` and `delta`.

Once green, switch back to **Wave 2** below.

---

# Lane L2 — Wave 2 — physics modelling + reproduction ledger seeding

**Branch:** `lane/L2-io-macros`
**Worktree:** `/Volumes/MyDrive/nnbar/nnbar/simulation-L2`
**Repo:** orchestration repo (this one)

**Wave 2 writable targets (disjoint from L0/L1/L3):**

- `docs/rebuild_plans/12_physics_list_audit.md`
- `docs/rebuild_plans/13_signal_model.md`
- `docs/rebuild_plans/14_background_models.md`
- `docs/rebuild_plans/15_material_budget.md`
- `docs/rebuild_plans/47_reproduction_ledger.md`
- `docs/thesis_reproduction_ledger.md` (NEW — the living ledger that
  plan 47 §4 specifies; create on first iteration)
- `data/ledger/rows.yml` (NEW — machine-readable rows; create on
  first iteration)

**Read-only:** everything else, especially plans 09 / 10 from Wave 1.

**External read paths** (thesis text — NOT under repo control):

- `/Users/billy/Desktop/projects/overleaf-hibeam-thesis/` — live PhD
  thesis (chapters 5–10 + 13 reproducibility appendix).
- `/Volumes/MyDrive/nnbar/phd thesis/thesis_extracted/` — frozen
  licentiate thesis (chapters 1–11).

These are LaTeX `.tex` files. Read them; do NOT edit them.

## Wave 2 goal — Part A: deepen physics-modelling plans 12–15

Each plan currently has the v0.1 skeleton. Wave 2 adds:

- **Plan 12 (physics list audit):** for every constructor registered
  in `PhysicsList.cc` per plan 07 §4 (Wave 1), add a justification
  paragraph and a citation to the Geant4 Physics Reference Manual or
  benchmark paper. Compare `FTFP_BERT` (no _HP) vs `FTFP_BERT_HP` for
  signal vs cosmic/beam-neutron samples; record the two-config
  rationale as a DEC-2026-MM-DD-N stub in the plan body.
- **Plan 13 (signal model):** populate §3 citation chain with
  bibliographic refs (Amsler & Myhrer 1991, Friedman & Gal 2007 are
  named in v0.1 — find their full references in
  `overleaf-hibeam-thesis/ref.bib`). Populate §4 alternative-model
  table with the two alternatives plus their reweighting recipe.
- **Plan 14 (background models):** confirm CRY parameter values §1.1
  (latitude, altitude, date) and surface as a DEC stub. Populate §2.1
  beam-neutron source decision (MCPL vs parameterised) as a DEC stub
  pending user input.
- **Plan 15 (material budget):** populate §1 inventory table with
  per-material X₀ and λ_I values. Use NIST PML tables for known
  materials; flag scintillator-plastic and lead-glass composition as
  "needs vendor citation" if the source isn't in the geometry
  builders. Use plan 07 §5 (Wave 1) for the volume list and
  `src/detector/*.cc` material definitions for compositions.

## Wave 2 goal — Part B: seed plan 47 reproduction ledger

Plan 47 specifies the ledger schema (§1), status states (§2), figure
equality protocol (§3), living-document protocol (§4). Wave 2 creates
the actual living ledger:

1. **Create `docs/thesis_reproduction_ledger.md`** with the §1 row
   schema as a header, then add at least **30 ledger rows** drawn
   from chapters 5–10 of the licentiate + PhD theses. Each row has:

   - `id`: e.g. `LIC-CH10-NUM-1`
   - `source`: chapter / section / figure or table reference
   - `thesis_value`: the exact number or "figure" placeholder
   - `reproducing.sample`: dataset id from plan 03 (use stub IDs from
     plans 20–23 for now; leave `not-attempted` status)
   - `reproducing.command`: the CLI invocation that would reproduce
     it (use the patterns from plan 10 macro inventory)
   - `status`: `not-attempted` (Wave 2 doesn't run anything; this is
     the inventory pass)
   - `leaf`: the plan 24 leaf id this row exercises
   - `decision_log`: any DEC entries that govern the row (often empty
     until DEC entries land)

2. **Create `data/ledger/rows.yml`** mirroring the markdown rows in
   machine-readable YAML form. One row per ledger entry. Use the
   schema in plan 47 §1.

3. **Update plan 47 §5** to point at the actual ledger file and
   replace the v0.1 stub list with a real count of rows added.

Source the rows by reading:
- `overleaf-hibeam-thesis/5_Detector_simulation.tex`
- `overleaf-hibeam-thesis/6_Signal_Bkg_simulation.tex`
- `overleaf-hibeam-thesis/7_Reconstruction.tex`
- `overleaf-hibeam-thesis/8_Object_Definition.tex`
- `overleaf-hibeam-thesis/9_Event_Variables.tex`
- `overleaf-hibeam-thesis/10_Event_selection.tex`
- (optional) `overleaf-hibeam-thesis/13_HIBEAM_reproducibility_appendix.tex`

Cross-check against the licentiate equivalents under
`/Volumes/MyDrive/nnbar/phd thesis/thesis_extracted/` when the PhD
chapter has the same content.

## Working rhythm (per iteration)

1. Re-read `docs/parallel-sessions.md` and this file.
2. Pick **one unit**: one plan section in 12–15, OR one chapter's
   ledger rows (e.g. "all numbers in Ch 8 §8.2") in plan 47.
3. Read the relevant source (Geant4 manual / thesis chapter / NIST
   PML).
4. Write the deepening / ledger rows.
5. Verify every edited file ≤ 500 lines. Split per L0's rules.
6. `git add` the touched file(s).
7. `git commit -m "docs(plan-XX): <unit>"` with `Plan: 12|...|47`,
   `Lane: L2` trailers.
8. `bash /Volumes/MyDrive/nnbar/nnbar/simulation/scripts/codex-supervisor/merge.sh lane/L2-io-macros`
9. Continue.

## Stop condition

- Plans 12–15 have citation chains, alternatives, and DEC stubs
  populated.
- `docs/thesis_reproduction_ledger.md` and `data/ledger/rows.yml`
  exist with **≥ 30 rows** drawn from chapters 5–10.

## Required reading (first iteration only)

- `CODING_STANDARDS.md`
- `docs/parallel-sessions.md`
- `docs/rebuild_plans/00_README.md`
- `docs/rebuild_plans/04_statistical_uncertainty.md`
  (uncertainty conventions ledger rows must cite)
- `docs/rebuild_plans/12_physics_list_audit.md` (current state)
- `docs/rebuild_plans/13_signal_model.md`
- `docs/rebuild_plans/14_background_models.md`
- `docs/rebuild_plans/15_material_budget.md`
- `docs/rebuild_plans/47_reproduction_ledger.md`

## Out of scope

- Plans 24–37 (L0/L1 own those).
- Plan 38 ladder code (L3 owns).
- Running any sample (sample regen is cluster-side).
- Editing the thesis files (read-only).

---

## Wave 3 follow-up (activate when Wave 2 stop condition is met)

When plans 12–15 and the chapter-5/6 ledger rows are done, move into
Wave 3 scope.

### Wave 3 writable targets (additional to L2)

- `docs/rebuild_plans/16_geometry_and_alignment.md`
- `docs/rebuild_plans/17_field_calibration.md`
- `docs/rebuild_plans/18_intercalibration.md`
- `docs/rebuild_plans/19_simulation_validation_suite.md`
- `docs/thesis_reproduction_ledger.md` (continue adding rows)
- `data/ledger/rows.yml` (continue)

### Wave 3 goal

**Part A — sim engineering plans 16–19**:

- Plan 16: list every alignment scenario with concrete σ values.
- Plan 17: lock the W-value DEC; record the chosen production value
  (23.6 vs 26.0 eV) with rationale.
- Plan 18: numbered closure procedure per intercalibration target
  (TPC↔Scint MIP, scintillator yield reconciliation, lead-glass
  linearity).
- Plan 19: list every test in `NNBAR_Detector/tests/` with its sanity-
  plot output, regression budget, CI matrix dimension.

**Part B — extend ledger to chapters 7–10**:

Read the remaining PhD chapters and add ≥30 more ledger rows from
`overleaf-hibeam-thesis/{7_Reconstruction,8_Object_Definition,9_Event_Variables,10_Event_selection}.tex`,
matching the row schema from Wave 2. Cross-check against the
licentiate equivalents.

### Wave 3 stop condition

Plans 16–19 populated; ledger has ≥60 total rows spanning all of
chapters 5–10. Lane keeps iterating to fill remaining gaps.

---

## Wave 4 — A+ closure (Stage B + C + D + E.2 for L2)

After Wave 3 stop condition, switch into Wave 4 scope.

### Wave 4 writable targets (additional to L2)

- `docs/rebuild_plans/62_detector_aging_and_radiation_damage.md` (NEW)
- `docs/rebuild_plans/63_calibration_drift_monitoring.md` (NEW)
- `docs/rebuild_plans/65_hibeam_phase1_combination.md` (NEW)
- `docs/governance/DECISION_LOG.md` (EXTEND — append newly promoted
  DEC entries when the user signs off on `DEC_BACKLOG.md`)
- `docs/governance/DECISION_LOG_INDEX.md` (EXTEND)
- Continue deepening 16, 17, 18, 19, 20, 22, 23 if any are still
  <150 lines.
- Continue ledger reproduction (plan 47) — target ≥30 rows
  reproduced or honest mismatch by end of Wave 4.
- `/Users/billy/Desktop/projects/overleaf-hibeam-thesis/ref.bib`
  (EXTEND — add `amsler:1991` and `friedman:2007` cite keys per
  Stage D.3 of the master plan).

### Wave 4 goal — Stage B + Stage C + Stage D + Stage E.2 (L2 share)

**Part A — finish skeleton-deepening.** Plans 17, 19, 20, 22, 23 are
≤120-line skeletons per the audit. Bring each to ≥150 lines with
actionable §-sections. Plus 16, 18 if not at A+ depth.

**Part B — write 3 new physics-study plans at A+ depth** (200–300
lines each):

- **62 detector aging + radiation damage**: ESS dose-rate model
  per subsystem (TPC, scintillator, lead glass, electronics);
  literature aging constants; expected response degradation over
  N years; mitigation (replacement schedule, recalibration); links
  to plan 01 limitation L3 and plan 18 intercalibration.
- **63 calibration drift monitoring**: drift-monitoring schedule
  across the run year; per-constant tolerance bands; failure
  triggers; link to plan 18 (initial intercal), plan 53 (CI),
  plan 66 (DQM, owned by L0).
- **65 HIBEAM Phase-1 combination**: Phase-1 result format
  (vertex-only TPC); combination strategy with NNBAR Phase-2
  (extracted limit comparison + joint likelihood); blinding
  protocol; correlation handling for shared systematics
  (TPC alignment, beam flux); link to plan 46 significance.

**Part C — Stage D.3 bibliography fixes.** Add the two missing
cite keys to `ref.bib`:
- `amsler:1991` — Amsler & Myhrer, "Low-energy antiproton physics",
  *Annu. Rev. Nucl. Part. Sci.* 41, 219 (1991).
- `friedman:2007` — Friedman & Gal, "In-medium nuclear interactions
  of low-energy hadrons", *Phys. Rep.* 452, 89 (2007).
Re-enable the 3 disabled weighted branches in plan 13 (search the
plan for `TODO(L2-bib)` markers).

**Part D — Stage E.2 ledger reproduction.** Continue the ledger
truthing rhythm from Wave 2.5. Target by Wave 4 stop: ≥30 rows
in `data/ledger/rows.yml` carry `status: reproduced` or
`status: mismatch` with non-empty `reproduced_value` and `delta`
fields. Each reproduced row anchors a thesis figure to the rebuild.

**Part E — DECISION_LOG maintenance.** When the user signs off on
DEC stubs in `DEC_BACKLOG.md` (e.g., DEC-2026-05-10-2 beam-neutron
source path, -4 alignment grid, -6 scintillator yield), append the
promoted entries to `DECISION_LOG.md` per plan 05 §3 schema and
update `DECISION_LOG_INDEX.md`. Do NOT promote without explicit
user sign-off in `DEC_BACKLOG.md`.

### Wave 4 stop condition

- Plans 16–23 all ≥150 lines.
- Plans 62, 63, 65 exist at v0.1 ≥200 lines.
- `ref.bib` contains `amsler:1991` and `friedman:2007`; plan 13's 3
  disabled branches re-enabled.
- ≥30 ledger rows reproduced or mismatch.
- All user-approved DEC stubs promoted into `DECISION_LOG.md`.
